# Copy this file to `.env` and fill in real values. Never commit `.env`.

# URL of your backend API — set to localhost for development,
# or your Render/Railway deployed URL for production.
# Example (prod): https://fintrak-api.onrender.com/api
VITE_API_BASE_URL=http://localhost:4000/api
